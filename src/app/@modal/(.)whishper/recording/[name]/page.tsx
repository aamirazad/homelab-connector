import { Modal } from "@/app/@modal/(.)paperless/document/[id]/modal";

export default function ModalAudioPreview({
  params,
}: {
  params: { id: number };
}) {
  return (
    <Modal>
      <div>Test</div>
    </Modal>
  );
}
